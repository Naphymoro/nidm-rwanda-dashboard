{
  "name": "ndim-cloudflare-worker",
  "version": "0.9.0-alpha.cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate:local": "wrangler d1 execute ndim-alpha-db --local --file=../schema/schema.sql",
    "d1:migrate:remote": "wrangler d1 execute ndim-alpha-db --remote --file=../schema/schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20260614.0",
    "typescript": "5.7.2",
    "wrangler": "4.20.5"
  }
}

